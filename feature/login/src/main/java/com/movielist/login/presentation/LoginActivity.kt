package com.movielist.login.presentation

import com.movielist.core.presentation.activity.BaseActivity
import com.movielist.login.R
import com.movielist.login.databinding.ActivityLoginBinding

class LoginActivity : BaseActivity<ActivityLoginBinding>(R.layout.activity_login) {
    override val navHostId: Int = 0
}